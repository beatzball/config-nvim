-- lazy.nvim plugins
return {
  -- most plugins are now in the lua/plugins folder as per recommended in the lazy.nvim docs
  -- https://github.com/folke/lazy.nvim?tab=readme-ov-file#-structuring-your-plugins
  --
  -- this is just an area to test new plugins out w/o going the extra mile for creating a file
}
